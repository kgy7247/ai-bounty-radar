# AI Bounty Radar

Public static service page for tracking small USDC/Web3 bounty opportunities and offering same-day bounty submission packs.

## Local preview

Open `docs/index.html` in a browser.

## Refresh data

```powershell
node scripts/refresh-opportunities.mjs
```

The script writes `docs/opportunities.json`.

